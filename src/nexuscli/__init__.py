"""nexuscli package (src-layout shim).

Provides a stable module path for tooling and `python -m nexuscli.*` entrypoints
without moving the existing codebase. See `nexuscli.orchestrator` for a thin
shim over the core orchestrator.
"""

